package com.zxin.module;

import java.io.BufferedReader;
import java.io.File;
import java.io.FileReader;

import org.apache.commons.io.FileUtils;
import org.apache.commons.lang.StringUtils;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.zxin.util.JsonUtil;

public class FileIOTest {

	private static Logger logger = LoggerFactory.getLogger(FileIOTest.class);
	
	public static void main(String[] args) throws Exception {
		
//		FileReader in = new FileReader(new File("d:/手机加密.log"));
//		BufferedReader buffer = new BufferedReader(in);
//		String mobile;
//		int i = 1;
//		while((mobile = buffer.readLine()) != null){
//			if(StringUtils.isNotBlank(mobile))
//			System.out.println(mobile +" "+ i++);
//			Thread.sleep(100);
//		}
	}
}

